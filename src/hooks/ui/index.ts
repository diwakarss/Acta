/**
 * UI Hooks
 * 
 * This directory contains hooks related to UI functionality.
 * These hooks handle theming, responsive design, animations, etc.
 */

// Export UI hooks
export { useAnimation } from './useAnimation';
export { useKeyboard } from './useKeyboard';
export { useTheme } from './useTheme';
export { useMarkdown } from './useMarkdown';

// As hooks are moved here, export them from this barrel file
// Example: export { default as useTheme } from './useTheme'; 