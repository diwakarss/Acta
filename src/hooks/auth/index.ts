/**
 * Authentication Hooks
 * 
 * This directory contains hooks related to authentication functionality.
 * These hooks handle user authentication, authorization, etc.
 */

// As hooks are moved here, export them from this barrel file
// Example: export { default as useAuth } from './useAuth'; 