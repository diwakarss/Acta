/**
 * Authentication Feature Components
 * 
 * This directory contains components specific to the authentication feature.
 * These components handle login, signup, password reset, etc.
 */

// Export auth components
export { AuthProvider, useAuth } from './AuthProvider';

// As components are moved here, export them from this barrel file
// Example: export { default as LoginForm } from './LoginForm'; 