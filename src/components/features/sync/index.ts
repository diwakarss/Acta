/**
 * Sync Feature Components
 * 
 * This directory contains components specific to the data synchronization feature.
 * These components handle sync status indicators, sync controls, etc.
 */

// Export sync components
export { default as SyncStatusIndicator } from './SyncStatusIndicator';

// As components are moved here, export them from this barrel file
// Example: export { default as SyncStatusIndicator } from './SyncStatusIndicator'; 