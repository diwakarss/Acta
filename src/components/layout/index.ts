/**
 * Layout Components
 * 
 * This directory contains components that define the layout structure of the application.
 * These components handle screen layout, navigation components, and structural elements.
 */

// As components are moved here, export them from this barrel file
// Example: export { default as Header } from './Header';
// Example: export { default as TabBar } from './TabBar'; 